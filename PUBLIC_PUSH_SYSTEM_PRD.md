# 公有推送系统产品需求文档 (PRD)

> **版本**: v1.0.0  
> **状态**: Production Grade  
> **最后更新**: 2026-02-18  
> **作者**: Exchange Product Team

---

## 一、文档概述

### 1.1 编写目的

本文档面向产品开发、架构、运维团队，明确公有推送系统（Public Push System）的产品需求、功能边界、性能指标和验收标准。

### 1.2 核心定位

**Public Push System ≠ Market Data Engine**

| 维度 | Market Data Engine | Public Push System |
|------|-------------------|-------------------|
| **职责** | 市场数据计算与聚合 | 市场数据分发与推送 |
| **核心任务** | OrderBook计算、K线生成、Ticker统计 | WebSocket管理、订阅管理、限流控制 |
| **性能关注** | 计算延迟 < 5ms | 推送延迟 < 10ms，并发 ≥ 100万连接 |
| **资源消耗** | CPU密集型 | 网络IO密集型 |

### 1.3 目标用户

| 用户角色 | 需求场景 | 使用方式 |
|---------|---------|---------|
| 量化交易用户 | 实时接收成交、深度变化 | WebSocket API |
| 普通交易用户 | 查看K线、最新价格 | WebSocket / REST |
| 第三方行情商 | 获取聚合行情数据 | WebSocket订阅 |
| 移动App | 实时推送价格提醒 | WebSocket长连接 |

---

## 二、需求背景

### 2.1 行业痛点

**Binance/OKX/Bybit的统一架构模式：**

```
┌─────────────────────────────────────────┐
│  ❌ 错误做法：合并计算与推送             │
│                                         │
│  ┌──────────────┐    WebSocket直连      │
│  │ 撮合引擎      │ ←→  客户端连接        │
│  └──────────────┘    （阻塞计算）        │
│                                         │
│  问题：                                   │
│  - 每来一个用户连接就影响撮合性能          │
│  - 无法水平扩容                           │
│  - 热点币种无法单独优化                    │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  ✅ 正确做法：计算与推送分层             │
│                                         │
│  ┌──────────────┐    ┌──────────────┐   │
│  │ 撮合引擎      │ →  │ 行情计算      │ → │ → Kafka
│  └──────────────┘    └──────────────┘   │
│                              ↓          │
│                       ┌──────────────┐   │
│                       │ 推送服务集群   │ ←→ 客户端连接
│                       └──────────────┘   │
│                                         │
│  优势：                                   │
│  - 计算层纯CPU，推送层纯网络              │
│  - 各自独立扩容                          │
│  - 热点symbol单独分片                     │
└─────────────────────────────────────────┘
```

### 2.2 竞品分析

| 特性 | Binance | OKX | Bybit | 本系统目标 |
|------|---------|-----|-------|-----------|
| WebSocket并发 | 500万+ | 300万+ | 200万+ | **100万+** |
| 推送延迟 | < 100ms | < 150ms | < 200ms | **< 50ms** |
| 批量合并 | 100ms | 100ms | 100ms | **10-100ms可调** |
| 订阅上限/连接 | 1024 | 512 | 512 | **1024** |
| 重连恢复 | 快照+增量 | 快照+增量 | 快照+增量 | **快照+增量** |
| 限流粒度 | IP+连接 | IP+连接 | IP+连接 | **IP+连接+用户** |

---

## 三、功能需求

### 3.1 核心功能矩阵

| 功能模块 | 功能点 | 优先级 | 状态 |
|---------|--------|-------|------|
| **连接管理** | WebSocket连接建立/关闭 | P0 | 已实现 |
| | 连接认证（API Key可选） | P1 | 待实现 |
| | 心跳保活机制 | P0 | 已实现 |
| | 僵尸连接自动清理 | P0 | 已实现 |
| **订阅管理** | 频道订阅/取消订阅 | P0 | 已实现 |
| | 批量订阅 | P1 | 待实现 |
| | 订阅数量限制 | P0 | 待实现 |
| | 自动订阅恢复 | P1 | 待实现 |
| **数据推送** | 实时成交推送 | P0 | 已实现 |
| | 深度增量推送 | P0 | 已实现 |
| | K线实时推送 | P0 | 已实现 |
| | 24h Ticker推送 | P0 | 已实现 |
| | 标记价格推送 | P0 | 已实现 |
| | 全市场Ticker推送 | P1 | 已实现 |
| **可靠性** | 快照+增量模式 | P0 | 已实现 |
| | 序号连续性检查 | P0 | 已实现 |
| | Gap检测与自动重建 | P0 | 已实现 |
| | 批量合并发送 | P0 | 待实现 |
| **限流与安全** | 连接数限流（IP） | P0 | 待实现 |
| | 订阅数限流（单连接） | P0 | 待实现 |
| | 消息频率限流 | P0 | 待实现 |
| | 异常连接熔断 | P1 | 待实现 |
| **可观测** | 连接数监控 | P0 | 待实现 |
| | 延迟监控 | P0 | 待实现 |
| | 消息量统计 | P0 | 待实现 |
| | 错误率监控 | P0 | 待实现 |

### 3.2 频道订阅规范

#### 3.2.1 频道命名规则

```
{dataType}.{symbol}
{dataType}.{interval}.{symbol}
```

#### 3.2.2 支持的频道类型

| 频道类型 | 格式 | 说明 | 推送频率 |
|---------|------|------|---------|
| **实时成交** | `trade.{symbol}` | 逐笔成交 | 实时 |
| **聚合成交** | `aggTrade.{symbol}` | 100ms聚合成交 | 100ms |
| **深度全量** | `depth.{symbol}` | 完整OrderBook | 100ms/有变化 |
| **深度增量** | `depth.{symbol}@100ms` | 增量深度（100ms） | 100ms |
| **K线** | `kline.{interval}.{symbol}` | 指定周期K线 | 实时 |
| **24h Ticker** | `ticker.{symbol}` | 24小时统计 | 1s |
| **全市场Ticker** | `ticker@arr` | 所有币种Ticker | 1s |
| **标记价格** | `markPrice.{symbol}` | 合约标记价格 | 1s |
| **全市场标记价** | `markPrice@arr` | 所有币种标记价 | 1s |

#### 3.2.3 订阅示例

```json
// 订阅请求
{
  "method": "SUBSCRIBE",
  "params": [
    "trade.BTCUSDT",
    "depth.BTCUSDT@100ms",
    "kline.1m.BTCUSDT",
    "ticker.BTCUSDT"
  ],
  "id": 1
}

// 订阅响应
{
  "result": "subscribed",
  "data": ["trade.BTCUSDT", "depth.BTCUSDT@100ms", "kline.1m.BTCUSDT", "ticker.BTCUSDT"],
  "id": 1
}
```

### 3.3 数据推送格式

#### 3.3.1 成交数据 (trade)

```json
{
  "stream": "trade.BTCUSDT",
  "data": {
    "e": "trade",           // 事件类型
    "E": 1704067200123,     // 事件时间
    "s": "BTCUSDT",         // 交易对
    "t": 123456789,         // 成交ID
    "p": "45000.50",        // 成交价格
    "q": "0.001",           // 成交数量
    "b": 12345678,          // 买方订单ID
    "a": 87654321,          // 卖方订单ID
    "T": 1704067200123,     // 成交时间
    "m": true,              // 是否买方maker
    "M": true               // 是否最优撮合
  }
}
```

#### 3.3.2 深度数据 (depth)

```json
{
  "stream": "depth.BTCUSDT@100ms",
  "data": {
    "e": "depthUpdate",     // 事件类型
    "E": 1704067200123,     // 事件时间
    "s": "BTCUSDT",         // 交易对
    "U": 702480515,         // 第一个更新ID
    "u": 702480520,         // 最后一个更新ID
    "pu": 702480514,        // 上一个更新ID（用于校验连续性）
    "b": [                  // 买盘更新
      ["45000.00", "1.500"],
      ["44999.50", "0.000"]  // 0表示删除
    ],
    "a": [                  // 卖盘更新
      ["45001.00", "2.000"],
      ["45001.50", "0.500"]
    ]
  }
}
```

#### 3.3.3 K线数据 (kline)

```json
{
  "stream": "kline.1m.BTCUSDT",
  "data": {
    "e": "kline",           // 事件类型
    "E": 1704067200123,     // 事件时间
    "s": "BTCUSDT",         // 交易对
    "k": {
      "t": 1704067200000,   // K线开始时间
      "T": 1704067259999,   // K线结束时间
      "s": "BTCUSDT",       // 交易对
      "i": "1m",            // 周期
      "f": 12345678,        // 第一笔成交ID
      "L": 12345700,        // 最后一笔成交ID
      "o": "45000.00",      // 开盘价
      "c": "45100.00",      // 收盘价
      "h": "45200.00",      // 最高价
      "l": "44900.00",      // 最低价
      "v": "100.500",       // 成交量
      "n": 23,              // 成交笔数
      "x": false,           // 是否完结
      "q": "4522500.00",    // 成交额
      "V": "50.000",        // 主动买入成交量
      "Q": "2250000.00"     // 主动买入成交额
    }
  }
}
```

#### 3.3.4 Ticker数据 (24h统计)

```json
{
  "stream": "ticker.BTCUSDT",
  "data": {
    "e": "24hrTicker",      // 事件类型
    "E": 1704067200123,     // 事件时间
    "s": "BTCUSDT",         // 交易对
    "p": "500.00",          // 24h价格变化
    "P": "1.12",            // 24h价格变化百分比
    "w": "44750.00",        // 加权平均价
    "x": "44500.00",        // 昨收
    "c": "45000.00",        // 最新价
    "Q": "0.100",           // 最新成交数量
    "b": "44999.50",        // 买一价
    "B": "1.000",           // 买一量
    "a": "45000.50",        // 卖一价
    "A": "0.500",           // 卖一量
    "o": "44500.00",        // 24h开盘价
    "h": "45500.00",        // 24h最高价
    "l": "44000.00",        // 24h最低价
    "v": "15000.000",       // 24h成交量
    "q": "671250000.00",    // 24h成交额
    "O": 1703980800000,     // 统计开始时间
    "C": 1704067200000,     // 统计结束时间
    "F": 12345000,          // 第一笔成交ID
    "L": 12360000,          // 最后一笔成交ID
    "n": 15000              // 成交笔数
  }
}
```

---

## 四、非功能需求

### 4.1 性能指标

| 指标 | 目标值 | 测试方法 | SLA |
|------|--------|---------|-----|
| **单节点WebSocket并发** | 10万连接 | JMeter/WebSocket压测 | 99.9% |
| **集群WebSocket并发** | 100万连接 | 生产流量回放 | 99.9% |
| **端到端推送延迟** | P99 < 50ms | 时间戳对比 | 99.99% |
| **消息吞吐量** | 100万msg/s | 生产者压测 | 99.9% |
| **CPU使用率** | < 70% | 监控告警 | 99% |
| **内存使用率** | < 80% | 监控告警 | 99% |
| **连接建立时间** | < 100ms | 网络延迟测试 | 99.9% |
| **心跳响应时间** | < 10ms | 心跳RTT测试 | 99.99% |

### 4.2 可靠性指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| **服务可用性** | 99.99% | 年度停机时间 < 52分钟 |
| **消息丢失率** | < 0.001% | 可通过Kafka重放恢复 |
| **消息重复率** | < 0.001% | 幂等消费保证 |
| **故障恢复时间** | < 30s | 自动故障转移 |
| **数据一致性** | 最终一致 | 序号检查保证 |

### 4.3 限流策略

#### 4.3.1 连接层限流

| 维度 | 限制值 | 触发动作 |
|------|--------|---------|
| **单IP连接数** | 50 | 拒绝新连接 |
| **单IP连接速率** | 10/分钟 | 限流，返回429 |
| **全局连接数** | 10万/节点 | 触发扩容 |

#### 4.3.2 订阅层限流

| 维度 | 限制值 | 触发动作 |
|------|--------|---------|
| **单连接订阅数** | 1024 | 拒绝订阅，返回错误 |
| **订阅变更频率** | 10/秒 | 限流，返回429 |

#### 4.3.3 消息层限流

| 维度 | 限制值 | 触发动作 |
|------|--------|---------|
| **单连接出站速率** | 10MB/s | 降级，丢弃非关键消息 |
| **单连接消息频率** | 1000条/秒 | 批量合并 |
| **全局出站带宽** | 1GB/s/节点 | 触发扩容 |

---

## 五、安全需求

### 5.1 传输安全

| 需求 | 实现方式 | 状态 |
|------|---------|------|
| **TLS加密** | WSS (WebSocket over TLS) | 必选 |
| **证书配置** | 支持多域名证书 | 必选 |
| **协议降级防护** | 禁止明文WS连接 | 必选 |

### 5.2 访问控制

| 需求 | 实现方式 | 状态 |
|------|---------|------|
| **IP白名单** | 配置允许访问的IP段 | 可选 |
| **IP黑名单** | 自动封禁异常IP | 必选 |
| **API Key认证** | 连接时可选验证 | 可选 |
| **签名校验** | HMAC-SHA256 | 可选 |

### 5.3 防护机制

| 威胁 | 防护措施 |
|------|---------|
| **DDoS攻击** | 连接速率限制、IP封禁、CDN前置 |
| **慢连接攻击** | 心跳超时检测、僵尸连接清理 |
| **资源耗尽** | 连接数上限、订阅数上限、内存限制 |
| **数据篡改** | TLS加密、消息签名（可选） |

---

## 六、接口规范

### 6.1 WebSocket连接

```
URL: wss://api.exchange.com/ws/v1/market
Protocol: 13 (RFC 6455)
```

### 6.2 心跳机制

```json
// 客户端发送心跳
{
  "ping": 1704067200123
}

// 服务端响应
{
  "pong": 1704067200123
}
```

**规则：**
- 心跳间隔：30秒（可配置）
- 超时未收到pong：关闭连接
- 服务端主动ping：客户端必须在5秒内响应

### 6.3 错误码

| 错误码 | 说明 | 处理建议 |
|--------|------|---------|
| 1000 | 正常关闭 | 无需处理 |
| 1001 | 服务端重启 | 客户端重连 |
| 1002 | 协议错误 | 检查消息格式 |
| 1006 | 异常关闭 | 客户端重连 |
| 1008 | 消息过大 | 减少订阅数量 |
| 1009 | 消息过大 | 减少订阅数量 |
| 1011 | 服务端错误 | 客户端重连 |
| 4001 | 订阅数超限 | 减少订阅 |
| 4002 | 频率超限 | 降低请求频率 |
| 4003 | IP被封禁 | 联系客服 |
| 4004 | 认证失败 | 检查API Key |

---

## 七、部署与运维

### 7.1 部署要求

| 环境 | 节点数 | 配置 |
|------|--------|------|
| **开发** | 1 | 4C8G |
| **测试** | 2 | 8C16G |
| **预发** | 3 | 16C32G |
| **生产** | ≥6 | 32C64G |

### 7.2 监控指标

| 指标类型 | 指标名称 | 告警阈值 |
|---------|---------|---------|
| **连接** | websocket_connections_total | > 8万/节点 |
| **连接** | websocket_connections_active | > 8万/节点 |
| **消息** | websocket_messages_sent_rate | 异常波动 |
| **消息** | websocket_messages_dropped_rate | > 0.01% |
| **延迟** | websocket_push_latency_p99 | > 100ms |
| **资源** | cpu_usage_percent | > 70% |
| **资源** | memory_usage_percent | > 80% |

### 7.3 日志规范

```
[时间] [级别] [traceId] [sessionId] [symbol] - 消息

示例：
2026-02-18 10:30:15.123 INFO [abc123] [sess_001] [BTCUSDT] - Connection established from 192.168.1.1
2026-02-18 10:30:15.456 INFO [abc123] [sess_001] [BTCUSDT] - Subscribed to [trade.BTCUSDT, depth.BTCUSDT@100ms]
2026-02-18 10:30:45.789 WARN [abc123] [sess_001] [BTCUSDT] - Heartbeat timeout, closing connection
```

---

## 八、验收标准

### 8.1 功能验收

- [ ] WebSocket连接建立/关闭正常
- [ ] 订阅/取消订阅功能正常
- [ ] 成交、深度、K线、Ticker数据推送正常
- [ ] 快照+增量模式工作正常
- [ ] 心跳机制工作正常
- [ ] 序号连续性检查正常
- [ ] Gap检测与重建正常

### 8.2 性能验收

- [ ] 单节点支持10万并发连接
- [ ] P99推送延迟 < 50ms
- [ ] 消息吞吐量 > 10万msg/s/节点
- [ ] 内存占用 < 4GB/万连接
- [ ] CPU使用率 < 70%（正常负载）

### 8.3 可靠性验收

- [ ] 服务可用性 > 99.99%（7×24运行）
- [ ] 故障转移时间 < 30s
- [ ] 消息丢失率 < 0.001%
- [ ] 滚动升级无感知

### 8.4 安全验收

- [ ] TLS加密正常工作
- [ ] 限流策略生效
- [ ] 异常连接被正确清理
- [ ] DDoS防护生效

---

## 九、附录

### 9.1 版本历史

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|---------|
| v1.0.0 | 2026-02-18 | Product Team | 初始版本 |

### 9.2 术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| 撮合引擎 | Match Engine | 执行订单撮合的核心系统 |
| 行情引擎 | Market Data Engine | 计算市场数据的系统 |
| 公有推送 | Public Push | 向所有用户广播行情数据 |
| 深度 | Depth | OrderBook的买盘和卖盘档位 |
| K线 | Kline/Candlestick | 价格走势图表数据 |
| Ticker | Ticker | 24小时价格统计数据 |

### 9.3 参考文档

- [KAFKA_DUAL_CHANNEL_ARCHITECTURE.md](./KAFKA_DUAL_CHANNEL_ARCHITECTURE.md)
- [PUBLIC_PUSH_ARCHITECTURE.md](./PUBLIC_PUSH_ARCHITECTURE.md)
- Binance WebSocket API文档: https://binance-docs.github.io/apidocs/
- OKX WebSocket API文档: https://www.okx.com/docs-v5/

---

**文档结束**

*本文档为内部机密资料，未经授权不得外传。*
