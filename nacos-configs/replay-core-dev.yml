# ============================================
# Replay Core (重放服务) 配置 (dev环境)
# 端口: 8087
# ============================================

server:
  port: 8087

spring:
  application:
    name: replay-core

# Logging配置
logging:
  level:
    com.exchange: INFO
