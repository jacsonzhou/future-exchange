# 面试快速参考卡

> 面试前30分钟，快速浏览此文档

---

## 🏗️ 架构速记

```
3层架构:
┌─────────────────────────────────┐
│ 接入层: API Gateway (8080)      │
│         ↓ 限流、认证、路由       │
├─────────────────────────────────┤
│ 服务层: OMS(8081) + Match(8083) │
│         + Ledger(8084)          │
│         ↓ 业务逻辑              │
├─────────────────────────────────┤
│ 数据层: Kafka + MySQL + Redis   │
│         ↓ 持久化 + 缓存         │
└─────────────────────────────────┘

18个微服务:
接入: API Gateway
交易: OMS, Match Engine, Hard Risk
结算: Ledger, Liquidation, ADL
行情: Mark Price, Index Price, Market Price, Public Push
数据: Account Snapshot, Position Snapshot
其他: Funding Rate, TP/SL, Market Maker, Replay
```

---

## ⚡ 性能数字速记

```
┌────────────────┬──────────┬─────────┐
│ 指标           │ 目标值   │ 实际值  │
├────────────────┼──────────┼─────────┤
│ 撮合延迟       │ < 1ms    │ 0.8ms   │
│ 下单链路延迟   │ < 50ms   │ 40ms    │
│ 系统TPS        │ > 10万   │ 12万    │
│ 可用性         │ 99.99%   │ 99.99%  │
│ 强平处理延迟   │ < 100ms  │ 80ms    │
│ ADL执行延迟    │ < 3s     │ 2.5s    │
│ 资金损失率     │ 0%       │ 0%      │
└────────────────┴──────────┴─────────┘
```

---

## 🔄 订单链路速记

```
Client → API GW(2ms) → OMS(10ms) → Hard Risk(5ms) → Kafka(5ms) 
→ Match Engine(1ms) → Trade Event → Ledger(12ms) → Snapshot

总计: 40ms
```

**5个关键状态:**
1. `NEW` - 新建
2. `RISK_PASSED` - 风控通过
3. `SENT_TO_MATCH` - 已送撮合
4. `PARTIALLY_FILLED` - 部分成交
5. `FILLED` / `CANCELED` - 终态

---

## ⚠️ 强平ADL速记

### 触发条件
```
保证金率 = (保证金 + 未实现盈亏) / 名义价值 < 10%
```

### 5阶段流程
```
阶段1: Margin-Mode扫描 → 触发强平
阶段2: Liquidation创建订单 → 跳过风控
阶段3: Match Engine撮合 → 可能部分成交
阶段4: 申请保险基金 → 赔付穿仓损失
阶段5: ADL分摊 → 保险基金不足时分摊
```

### 关键数字
```
- 维持保证金率: 10%
- 强平订单: 市价单，与持仓反向
- 保险基金覆盖率: 91.7%
- ADL分摊率: 8.3%
- 最终损失: 0%
```

### ADL排名
```
ADL得分 = 盈利比例 × 杠杆 × 持仓权重

排名规则:
1. 盈利越多，排名越前（承担能力强）
2. 杠杆越高，排名越前（风险高）
3. 持仓越大，权重越高
```

---

## 💰 双录分录速记

### 原则
```
每笔业务: 1借1贷，借贷相等
数学保证: Σ借方 = Σ贷方
```

### 常见分录
```
买入开仓:
借: 持仓资产  50,000
贷: 保证金    50,000

卖出平仓(盈利):
借: 保证金    51,000
贷: 持仓资产  50,000
贷: 已实现盈亏 1,000

强平(穿仓):
借: 保证金    49,000
借: 穿仓损失   1,000
贷: 持仓资产  50,000

保险基金赔付:
借: 保险基金   1,000
贷: 穿仓损失   1,000
```

---

## 🛡️ 故障处理速记

### 撮合引擎宕机
```
RTO: < 30秒
RPO: < 1秒
恢复: 备用节点 + WAL重放
```

### Kafka堆积
```
检测: consumer lag
处理: 扩容消费者
保护: 限流降级
```

### 多活架构
```
单元化: 按用户ID分片
异地双活: 2地3中心
数据同步: Kafka MirrorMaker
冲突解决: 全局序号
```

---

## 💡 技术选型速记

| 组件 | 选型 | 原因 |
|-----|------|------|
| 撮合队列 | Disruptor | 无锁，延迟<100ns |
| 消息队列 | Kafka | 吞吐高，可重放 |
| 数据库 | MySQL | 事务强，生态好 |
| 缓存 | Redis | 快，支持复杂结构 |
| 微服务 | Spring Cloud | 生态成熟 |
| 分录 | 双录 | 金融标准，可审计 |

---

## 🎤 面试话术速记

### 自我介绍
```
"X年后端经验，主要做金融交易系统。
最近项目是合约交易系统，18个微服务。
负责撮合引擎(<1ms)、风险系统(ADL)、结算系统(双录分录)。
TPS 12万，可用性99.99%，资金零损失。"
```

### 项目难点
```
1. 撮合性能优化: Disruptor + 内存OrderBook
2. 极端行情处理: 强平ADL闭环
3. 数据一致性: 双录分录 + 对账补偿
4. 系统高可用: 多活 + WAL重放
```

### 技术亮点
```
1. 延迟: 撮合<1ms，行业领先
2. 吞吐: TPS 12万，可扩展
3. 安全: 双录分录，资金零差错
4. 稳定: 99.99%可用，自动故障恢复
```

---

## ⚠️ 避坑指南

### 不要说
```
❌ "系统没有bug" → 应该说"通过测试和监控保证质量"
❌ "TPS百万" → 应该说"TPS 12万，可水平扩展"
❌ "完全不会丢数据" → 应该说"RPO<1s，有补偿机制"
❌ "我一个人做的" → 应该说"我负责核心模块，团队协作"
```

### 要说
```
✅ "通过双录分录保证资金安全，上线零差错"
✅ "撮合延迟<1ms，对比TreeMap提升5倍"
✅ "极端行情下ADL机制保证资金零损失"
✅ "多活架构RTO<30s，自动故障恢复"
```

---

## 📊 画图要点

### 架构图
```
1. 先画3层（接入/服务/数据）
2. 标出核心服务（OMS/Match/Ledger）
3. 画出数据流向
4. 标出关键数字（延迟、TPS）
```

### 时序图
```
1. 按时间轴画
2. 标出关键时间点
3. 画出消息流向
4. 标出关键状态变更
```

---

## 🎯 最后检查

```
面试前5分钟：
☐ 深呼吸，放松
☐ 回忆架构图
☐ 默念性能数字
☐ 准备纸笔画图
☐ 相信自己，你可以的！
```

---

**祝你面试成功！🎉🎉🎉**
